VITE_WORKOS_CLIENT_ID=client_123456789
# Optional: set this to your WorkOS custom authentication API domain in production.
# If omitted, the app falls back to AuthKit devMode and stores the refresh token in localStorage.
VITE_WORKOS_API_HOSTNAME=
